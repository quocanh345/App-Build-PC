User:
- Xác thực
- Định danh

Features:
- schema first, code sau
- CPU, Ram, SSD, VGA, ... (entities, dto, TS) -> zero trust. thông số i5 12450H. password, CPU cores, hz 


backend <-> frontend app web
HTTP:
header : update
body -> { object: password }

{ username: true }

request
get post update delete ...
endpoint get image, get in4, get messages

https://facebook/posts/user4953 -> URL

https://facebook/info/user4953

https://facebook/live/user4953


REST -> N endpoint
GraphQL -> 1 endpoint /graphQL -> query parse 1000 req 

POST 
body { ..... }

controller N URL

DTO data transfer obj

3.DTO
- input DTO
bảo backend khỏi những req rác
req username password
cm mm lít everest

Entities: <-> ORM -> typeORM

RAM 
GB:
Hãng:
HZ:
.... -> Database -> table - ORM -> viết sql thuần
sql -> code hóa SELEct SEX FROM ...
JS: find.user.({ username }); -> SQL 
CREATE ... 

{ 
  ram: int,
  from: string,
  hz: float,
}

- output DTO
{ GB:
Hãng:
HZ: }
RAM -> password

NestJS 

hard code dễ test

PostgreSQL, SQL server, MongoDB


inject

repository 
query
connector
....

phone:
inch
chip
ram
rom
oled

controller 


DB -> 80% problems 
DB -> Backend -> Frontend

ORM code hóa sql
entities -> database
API -> query sql REST, GraphQL

application programing inteface 

JS <-> TypeScript